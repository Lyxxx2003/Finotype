Create card like component with blog like (a hook image composing 1/2 of the card, with title, and a subtitle/description). Populate these cards in resources/page.tsx, the number of these cards should be changeable, so create a variable for easy later changes (add comment TODO). The image will be dynamically generated from /public/mascots, you could somehow combine/add things on top of the images, but with some logic, so e.g. 1st card with this variation, 2nd with this, it has to have a pattern (highlight). When clicking on the card, guide to the real blog.

IMPORTANT
Read globals.css to follow current design and dark/light mode.
Remember i18n.
Populate some test texts for the title/subtitle or description and the real blog, make it as clear varaibles and comment as TODO so that I could revise later.

Navbar should have resources tab for all mode (all isPro/isStandard/or default). Iterate untile all features satisfied and no errors